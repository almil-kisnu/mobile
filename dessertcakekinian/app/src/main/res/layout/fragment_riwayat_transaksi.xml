<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:background="?attr/android:windowBackground"
    tools:context=".fragment.RiwayatTransaksiFragment">

    <LinearLayout
        android:id="@+id/headerLayoutRiwayat"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"

        android:background="?attr/android:windowBackground"
        android:elevation="4dp"
        android:paddingHorizontal="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/btnBackRiwayat"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_arrow_back"
            android:background="?attr/selectableItemBackgroundBorderless"

            app:tint="?attr/android:textColor"
            android:contentDescription="Kembali" />

        <TextView
            android:id="@+id/tvJudulRiwayat"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Riwayat Transaksi"

            android:textColor="?attr/android:textColor"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginStart="-40dp" />

    </LinearLayout>

    <EditText
        android:id="@+id/etSearchRiwayat"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp"

        android:background="@drawable/bg_search"
        android:hint="Cari no. transaksi atau item"
        android:paddingHorizontal="16dp"
        android:paddingVertical="12dp"
        android:drawableStart="@drawable/ic_searchbar"
        android:drawablePadding="8dp"

        android:textColor="?attr/android:textColor"
        android:textColorHint="?android:attr/textColorHint"
        android:drawableTint="?android:attr/textColorHint"

        android:textSize="14sp"
        android:elevation="2dp"
        app:layout_constraintEnd_toStartOf="@+id/cardFilterRiwayat"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/headerLayoutRiwayat" />

    <androidx.cardview.widget.CardView
        android:id="@+id/cardFilterRiwayat"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:layout_marginEnd="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp"

        app:cardBackgroundColor="?attr/colorPrimaryVariant"

        app:layout_constraintBottom_toBottomOf="@id/etSearchRiwayat"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/etSearchRiwayat">

        <ImageButton
            android:id="@+id/btnFilterRiwayat"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_filter_off"
            android:padding="10dp"
            android:scaleType="centerInside"
            tools:ignore="ContentDescription" />
    </androidx.cardview.widget.CardView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvRiwayat"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:clipToPadding="false"
        android:paddingHorizontal="12dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etSearchRiwayat"
        tools:listitem="@layout/fragment_item_riwayat_transaksi" />

</androidx.constraintlayout.widget.ConstraintLayout>