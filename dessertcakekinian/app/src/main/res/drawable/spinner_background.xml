<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@android:color/white" />

    <!-- Border -->
    <stroke
        android:width="1dp"
        android:color="#BDBDBD" /> <!-- abu-abu lembut -->

    <!-- Rounded corner -->
    <corners android:radius="12dp" />

    <!-- Padding dalam -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
